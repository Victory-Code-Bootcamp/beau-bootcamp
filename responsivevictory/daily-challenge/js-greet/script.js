
let hour = () => new Date().getHours()
let greet = hour < 12 && hour === 7 ? 'Time for work' : hour < 12 ? "Good Morning" : hour > 12 && hour === 18? 'Dinner time!' : 'Good Evening'

console.log(greet)

let calculateDiscount = (num1, num2) => {
    
}

